# KAF 国王-Agent 框架 v5.4 发布说明

> 发布时间：2026-08-07
> 真源：https://github.com/lsjpp2/king-agent-swarm
> 上一版标签：v5.3

## 一句话

v5.4 在 v5.3（治理层 + 动态国王）之上，补上**进智脊柱（Cognition Spine）**——把"可进化"从只写不读的坟场，变成可运行、可闭环、可对齐检验的**智慧层**。防错脊柱管"别蠢"，进智脊柱管"变慧"，两者正交、后者受前者约束。

## 核心补充（v5.4 进智脊柱）

- **五零件**：
  1. ① 反模式库 Anti-pattern —— 记录"绝对不要做" + 触发条件 + 正确替代；种子来自 Claw 会话清理事故 `8f6d42dc`。
  2. ② 检索注入 Retrieval Inject —— 任务起点主动拉相关反模式 + 高置信经验进上下文（解决只写不读）。
  3. ③ 经验蒸馏 Distillation（带置信度）—— 任务收尾压成 `{context,action,outcome,confidence}`；低置信不注入防噪声。
  4. ④ 决策校准引擎 Calibration —— 相似决策比对历史 `outcome/confidence`，标注误校准。
  5. ⑤ 元认知门控 deliberate() —— 高利害动作前自问"是否重蹈覆辙"，命中→正确替代或升国王确认；不高于国王否决权。
- **Loop Driver 闭环自修（v5.4 交付质量闭环）**：候选产出 → 后台对齐检验（逐条比对指令）→ 未达标自动修订 → 再检验 → 收敛交付；循环本身喂回蒸馏，修订策略越跑越准。
- **对齐度阈值三档（国王已确认 2026-08-07）**：
  - 硬阈值（可量化指令，如"归档 N 条且均经 conversation_search 零命中"）= 100% 对齐，差一项即修订，无需确认自动收敛；
  - 软阈值（半量化，如"标题要一目了然"）= 关键约束零违反 + 主要意图对齐 ≥ 80%，达标即收敛、每轮留痕备国王抽查；
  - 国王兜底（模糊，如"整理好看点"）= 不设自动阈值，跑 ≤1 轮基础对齐后**升国王确认**，不擅自定稿。
- **实证锚点**：Claw 会话清理事件 `8f6d42dc`——数据零丢失但一刀切 / 数字反复 / 标题 bug 重复 4+ 次，证明"防错强 ≠ 有智慧"；首条反模式种子即来自此。

## 能力对照（v5.0 → v5.4）

| 能力 | v5.0 | v5.1 | v5.2 | v5.3 | v5.4 |
|:---|:---:|:---:|:---:|:---:|:---:|
| 宪法 / 520 护栏 | ✓ | ✓ | ✓ | ✓ | ✓ |
| 模型经济学路由 | – | ✓ | ✓ | ✓ | ✓ |
| 多视角审查闭环 | – | ✓ | ✓ | ✓ | ✓ |
| 共享账本持久化 | – | ✓ | ✓ | ✓ | ✓ |
| 治理层 + kill-switch | – | – | – | **✓** | **✓** |
| 防篡改审计链 | – | – | – | **✓** | **✓** |
| agent HMAC 背书 | – | – | – | **✓** | **✓** |
| 动态国王 | 硬编码 | 硬编码 | 硬编码 | **✓** | **✓** |
| 图解 01-08 + 离线页 | 部分 | 部分 | 01-03 | **✓** | **✓** |
| **反模式库** | – | – | – | – | **✓** |
| **检索注入** | – | – | – | – | **✓** |
| **经验蒸馏（置信度）** | – | – | – | – | **✓** |
| **决策校准引擎** | – | – | – | – | **✓** |
| **deliberate() 门控** | – | – | – | – | **✓** |
| **闭环自修 Loop Driver** | – | – | – | – | **✓** |
| **图解 12-14（进智/闭环）** | – | – | – | – | **✓** |

## 架构文档与图解

- `docs/architecture_v5.4.md`：完整 5.4 架构（七层含进智层 + 进智五零件 + 闭环自修 + 阈值分级 + 与治理层关系 + State/Message/Security 模型更新）。
- `diagrams/12-architecture-v5.4.svg`（七层架构，含进智层，深色风格对齐 04）、`13-cognition-full.svg`（进智全景 + 数据流）、`14-loop-closure.svg`（闭环 + 阈值三档）。
- 进智脊柱设计提案（背景推导与国王确认记录）：`docs/cognition_spine_proposal.md`。

## 落地节奏（代码分阶段，宣称=实现）

- **v5.3.1（地基）**：①②⑤ —— 反模式种子 `anti_patterns.jsonl` + 检索注入 `retrieval_inject` + `deliberate()` 钩子。
- **v5.4（闭环）**：③④ + Loop Driver —— 经验蒸馏（置信度）/ 决策校准引擎 / 后台对齐检验与修订循环（需经验沉淀才有意义）。
- 本发布为**架构与规范定稿**；代码模块按上述节奏落地，不提前宣称已实现。

## 远程复制者指引

```bash
git clone https://github.com/lsjpp2/king-agent-swarm.git
cd king-agent-swarm
# 默认你就是国王（当前 OS 用户），无需改代码
export KAF_KING=你的名字        # 或 kaf_config.json 写 {"king":"你的名字"}
python kaf/kaf.py check
```

## 校验

- 共享归档：`D:/Agent集群共享/国王技能KAF/v5.4/`（git archive 真快照）。
- 集群生效仓：`C:/Users/山禾/.workbuddy/skills/国王-Agent蜂群/` 已同步（SKILL.md / constitution.json / diagrams / docs）。
- git tag `v5.4` 指向本发布提交；远程 `master` 已推送（SHA 回验一致）。
- `constitution.json` `version=5.4` 且含 `cognition` 节；`SKILL.md` 标题/理念/七层架构/图解引用/进智节 均已升级。

## 与 v5.3 兼容

- 治理层、动态国王、520 护栏、记忆隔离**全部向后兼容**；进智脊柱为新增正交层，不改动既有强制逻辑。
- 升级方式：复制本快照覆盖即可。`constitution.json` 仅新增 `cognition` 节，旧客户端忽略未知节，无破坏。
